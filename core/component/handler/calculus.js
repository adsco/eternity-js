function Calculus(dataProvider){
    var _dataProvider = null;
    
    /**
     * Constructor
     */
    var _construct = function(dataProvider){
        
    };
    
    this.acceptable = function(element, e){
        
    };

    this.handle = function(element, e){
        
    };
    
    _construct.call(this, dataProvider);
}